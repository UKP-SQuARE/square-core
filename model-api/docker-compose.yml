# only for dev work

version: "3.3"

services:
  traefik:
    image: traefik:v2.8.7
    container_name: traefik
    command:
      - --api.insecure=true
      - --providers.docker=true
      - --providers.docker.exposedbydefault=false
      - --entrypoints.web.address=:80
      - --accesslog=true
      - --accesslog.format=json
      - --accesslog.filepath=/var/log/traefik/access.log.json
      # Enable https port 443
      - --entrypoints.websecure.address=:443
      - --certificatesresolvers.le.acme.tlschallenge=true
      # Uncomment staging certs for testing
      - --certificatesresolvers.le.acme.caserver=https://acme-staging-v02.api.letsencrypt.org/directory
      - --certificatesresolvers.le.acme.email=sachdeva@ukp.informatik.tu-darmstadt.de
      - --certificatesresolvers.le.acme.storage=/certificates/acme.json
      # Enable dashboard and logging
      - --api.dashboard=true
      - --log.level=DEBUG
    ports:
      - "8989:80"  # http port
      - "8443:443"  # https port
      - "8080:8080"  # web UI port
    labels:
      - "traefik.enable=true"
      # Global redirect to https
      - "traefik.http.routers.http-catchall.rule=hostregexp(`{host:.+}`)"
      - "traefik.http.routers.http-catchall.entrypoints=web"
      - "traefik.http.routers.http-catchall.middlewares=https-redirect"

      # Middleware redirect from HTTP to HTTPS
      - "traefik.http.middlewares.https-redirect.redirectscheme.scheme=https"
      - "traefik.http.middlewares.https-redirect.redirectscheme.port=8443"
      - "traefik.http.middlewares.https-redirect.redirectscheme.permanent=true"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      # mount volume to store certificates
      - traefik-public-certificates:/certificates

  rabbit:
    hostname: rabbit
    image: rabbitmq:3.9.14-management
    ports:
      - 5672:5672
      - 15672:15672
#    env_file:
#      - ./management_server/.env
    env_file:
      - ../model-manager/.env
#    volumes:
#      - ./management_server/rabbitmq/rabbitmq.conf:/etc/rabbitmq/rabbitmq.conf
#      - ./management_server/rabbitmq/definitions.json:/etc/rabbitmq/definitions.json

  redis:
    hostname: redis
    image: redis:latest
    ports:
      - '6379:6379'
#    env_file:
#      - .env
    env_file:
      - ../model-manager/.env
    command: [ "redis-server", "--requirepass ${REDIS_PASSWORD}" ]

#    volumes:
#      - square-redis-data:/square/redis/data:rw
#      - ./management_server/redis/redis.conf:/etc/redis/redis.conf

  maintaining_worker:
#    image: ukpsquare/square-model-management:latest
#    build: ./management_server
    build:
      context: ../model-manager
      dockerfile: Dockerfile
    container_name: maintaining_worker
    command: celery -A tasks worker --loglevel=info
    volumes:
      - ./:/usr/src/app
      - /var/run/docker.sock:/var/run/docker.sock
#    env_file:
#      - ./management_server/.env
    env_file:
      - ../model-manager/.env
    environment:
      - DOCKER_HOST_URL=https://172.17.0.1:8443
      - MODEL_API_IMAGE=ukpsquare/square-model-api-v2:latest
      - MONGO_INITDB_ROOT_USERNAME=root
      - VERIFY_SSL=0
      - KEYCLOAK_BASE_URL=https://square.ukp-lab.de
      - REALM=square
      - CLIENT_ID=models
    links:
      - rabbit
      - redis
    depends_on:
      - maintaining
      - rabbit
      - redis

  maintaining:
#    image: ukpsquare/square-model-management:latest
    command: uvicorn main:app --host 0.0.0.0 --port 9001 --log-config logging.conf
    container_name: maintaining
#    build: ./management_server
    build:
      context: ../model-manager
      dockerfile: Dockerfile
    ports:
      - 9001:9001
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    links:
      - mongo
    depends_on:
      - mongo
#    env_file:
#      - ./management_server/.env
    env_file:
      -  ../model-manager/.env
    environment:
      - DOCKER_HOST_URL=https://172.17.0.1:8443
      - MODEL_API_IMAGE=ukpsquare/square-model-api-v2:latest
      - VERIFY_SSL=0
      - WEB_CONCURRENCY=1
      - ONNX_VOLUME=square-model-inference-api_onnx-models
      - KEYCLOAK_BASE_URL=https://square.ukp-lab.de
      - REALM=square
      - CLIENT_ID=models
      - CONFIG_PATH=/model_configs
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.maintaining.rule=PathPrefix(`/api/models`)"
      - "traefik.http.routers.maintaining.entrypoints=websecure"
      - "traefik.http.routers.maintaining.tls=true"
      - "traefik.http.routers.maintaining.tls.certresolver=le"
      - "traefik.http.routers.maintaining.middlewares=maintaining-stripprefix,maintaining-addprefix"
      - "traefik.http.middlewares.maintaining-stripprefix.stripPrefixRegex.regex=/api/[a-zA-Z0-9_-]+"
      - "traefik.http.middlewares.maintaining-addprefix.addPrefix.prefix=/api"


##################################################################
# --------- Example config for transformer model server ---------#
##################################################################
  main_model:
#    image: ukpsquare/square-model-api-v1:latest
    build:
      context: ./inference_api
      dockerfile: ./dockerfiles/transformer/Dockerfile
    container_name: main_model
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
    env_file:
      - inference_api/model_configs/.env.dpr
      - .env
    environment:
      - WEB_CONCURRENCY=1
      - KEYCLOAK_BASE_URL=https://square.ukp-lab.de
      - QUEUE=facebook-dpr-question_encoder-single-nq-base
      - CONFIG_PATH=/model_configs
#      - TEST=1   # for testing model config
    volumes:
      - ./.cache/:/etc/huggingface/.cache/
      - model_configs:/model_configs
#      - onnx-models:/onnx_models
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.model-dpr.rule=PathPrefix(`/api/main`)"
      - "traefik.http.routers.model-dpr.entrypoints=websecure"
      - "traefik.http.routers.model-dpr.tls=true"
      - "traefik.http.routers.model-dpr.tls.certresolver=le"
      - "traefik.http.routers.model-dpr.middlewares=model-dpr-stripprefix,model-dpr-addprefix"
      - "traefik.http.middlewares.model-dpr-stripprefix.stripprefix.prefixes=/api/main"
      - "traefik.http.middlewares.model-dpr-addprefix.addPrefix.prefix=/api"


  mongo:
    hostname: mongo
    image: mongo:5.0.4
    restart: always
    volumes:
      - mongo-data:/data/db
    ports:
      - 27017:27017
    env_file:
      - ../model-manager/.env
#    env_file:
#      - management_server/.env

  mongo-express:
    image: mongo-express:latest
    restart: always
#    env_file:
#      - ./management_server/.env
    env_file:
      - ../model-manager/.env
    ports:
      - 8081:8081

###########################################################
# --------- Example config for onnx model server ---------#
###########################################################
#  bert_onnx:
##    image: ukpsquare/square-model-api-v1:latest
#    build:
#      context: ./inference_api
#      dockerfile: ./dockerfiles/onnx/Dockerfile
#    env_file:
#      - ./inference_api/model_configs/.env.bert_onnx
#      - .env
#    environment:
#      - WEB_CONCURRENCY=1
#      - KEYCLOAK_BASE_URL=https://square.ukp.informatik.tu-darmstadt.de
#    volumes:
#      - ./.cache/:/etc/onnx/.cache/
#      - onnx-models:/onnx_models
#      - model_configs:/model_configs
#    labels:
#      - "traefik.enable=true"
#      - "traefik.http.routers.model-bert-onnx.rule=PathPrefix(`/api/bert-onnx`)"
#      - "traefik.http.routers.model-bert-onnx.entrypoints=websecure"
#      - "traefik.http.routers.model-bert-onnx.tls=true"
#      - "traefik.http.routers.model-bert-onnx.tls.certresolver=le"
#      - "traefik.http.routers.model-bert-onnx.middlewares=model-bert-onnx-stripprefix,model-bert-onnx-addprefix"
#      - "traefik.http.middlewares.model-bert-onnx-stripprefix.stripprefix.prefixes=/api/bert-onnx"
#      - "traefik.http.middlewares.model-bert-onnx-addprefix.addPrefix.prefix=/api"

  bert_onnx_worker:
    build:
      context: ./inference_api
      dockerfile: ./dockerfiles/onnx/Dockerfile
    command: celery -A tasks worker -Q bert_onnx --loglevel=info
    volumes:
      - ./:/usr/src/app
      - /var/run/docker.sock:/var/run/docker.sock
      - model_configs:/model_configs
      - onnx-models:/onnx_models
    env_file:
      - ./inference_api/model_configs/.env.bert_onnx
      - .env
    environment:
      - DOCKER_HOST_URL=https://172.17.0.1:8443
      - VERIFY_SSL=0
      - KEYCLOAK_BASE_URL=https://square.ukp-lab.de
      - REALM=square
      - CLIENT_ID=models
      - QUEUE=bert_onnx
      - CONFIG_PATH=/model_configs
    links:
      - rabbit
      - redis
    depends_on:
      - maintaining
      - rabbit
      - redis
  distilbert_onnx_worker:
    build:
      context: ./inference_api
      dockerfile: ./dockerfiles/onnx/Dockerfile
    command: celery -A tasks worker -Q distilbert_onnx --loglevel=info
    volumes:
      - ./:/usr/src/app
      - /var/run/docker.sock:/var/run/docker.sock
      - model_configs:/model_configs
      - onnx-models:/onnx_models
    env_file:
      - ./inference_api/model_configs/.env.distilbert_onnx_qa
      - .env
    environment:
      - DOCKER_HOST_URL=https://172.17.0.1:8443
      - VERIFY_SSL=0
      - KEYCLOAK_BASE_URL=https://square.ukp-lab.de
      - REALM=square
      - CLIENT_ID=models
      - QUEUE=distilbert_onnx
      - CONFIG_PATH=/model_configs
    links:
      - rabbit
      - redis
    depends_on:
      - maintaining
      - rabbit
      - redis
  distilbert_onnx_seq_worker:
    build:
      context: ./inference_api
      dockerfile: ./dockerfiles/onnx/Dockerfile
    command: celery -A tasks worker -Q distilbert_onnx_seq --loglevel=info
    volumes:
      - ./:/usr/src/app
      - /var/run/docker.sock:/var/run/docker.sock
      - model_configs:/model_configs
      - onnx-models:/onnx_models
    env_file:
      - ./inference_api/model_configs/.env.distilbert_onnx_seq
      - .env
    environment:
      - DOCKER_HOST_URL=https://172.17.0.1:8443
      - VERIFY_SSL=0
      - KEYCLOAK_BASE_URL=https://square.ukp-lab.de
      - REALM=square
      - CLIENT_ID=models
      - QUEUE=distilbert_onnx_seq
      - CONFIG_PATH=/model_configs
    links:
      - rabbit
      - redis
    depends_on:
      - maintaining
      - rabbit
      - redis
  bert_onnx_tok_worker:
    build:
      context: ./inference_api
      dockerfile: ./dockerfiles/onnx/Dockerfile
    command: celery -A tasks worker -Q bert_onnx_tok --loglevel=info
    volumes:
      - ./:/usr/src/app
      - /var/run/docker.sock:/var/run/docker.sock
      - model_configs:/model_configs
      - onnx-models:/onnx_models
    env_file:
      - ./inference_api/model_configs/.env.bert_onnx_tok
      - .env
    environment:
      - DOCKER_HOST_URL=https://172.17.0.1:8443
      - VERIFY_SSL=0
      - KEYCLOAK_BASE_URL=https://square.ukp-lab.de
      - REALM=square
      - CLIENT_ID=models
      - QUEUE=bert_onnx_tok
      - CONFIG_PATH=/model_configs
    links:
      - rabbit
      - redis
    depends_on:
      - maintaining
      - rabbit
      - redis

  t5_onnx_gen_worker:
    build:
      context: ./inference_api
      dockerfile: ./dockerfiles/onnx/Dockerfile
    command: celery -A tasks worker -Q t5_onnx_gen --loglevel=info
    volumes:
      - ./:/usr/src/app
      - /var/run/docker.sock:/var/run/docker.sock
      - model_configs:/model_configs
      - onnx-models:/onnx_models
    env_file:
      - ./inference_api/model_configs/.env.t5_onnx_gen
      - .env
    environment:
      - DOCKER_HOST_URL=https://172.17.0.1:8443
      - VERIFY_SSL=0
      - KEYCLOAK_BASE_URL=https://square.ukp-lab.de
      - REALM=square
      - CLIENT_ID=models
      - QUEUE=t5_onnx_gen
      - CONFIG_PATH=/model_configs
    links:
      - rabbit
      - redis
    depends_on:
      - maintaining
      - rabbit
      - redis
#
#  bart_onnx_worker:
#    build:
#      context: ./inference_api
#      dockerfile: ./dockerfiles/onnx/Dockerfile
#    command: celery -A tasks worker -Q bart_onnx --loglevel=info
#    volumes:
#      - ./:/usr/src/app
#      - /var/run/docker.sock:/var/run/docker.sock
#      - model_configs:/model_configs
#      - onnx-models:/onnx_models
#    env_file:
#      - ./inference_api/model_configs/.env.bart_onnx
#      - .env
#    environment:
#      - DOCKER_HOST_URL=https://172.17.0.1:8443
#      - VERIFY_SSL=0
#      - KEYCLOAK_BASE_URL=https://square.ukp-lab.de
#      - REALM=square
#      - CLIENT_ID=models
#      - QUEUE=bart_onnx
#      - CONFIG_PATH=/model_configs
#    links:
#      - rabbit
#      - redis
#    depends_on:
#      - maintaining
#      - rabbit
#      - redis

volumes:
  traefik-public-certificates:

  mongo-data:
    driver: local

#  square-redis-data:
#    driver: local

  # onnx-model folder on the vm
#
  onnx-models:
    driver: local
    driver_opts:
      o: bind
      type: none
      device: /Users/zhanghao/Desktop/UKP/v4-onnx/onnx_models


#  model_storage:
#    driver: local
#    driver_opts:
#      o: bind
#      type: none
#      device: /home/rachneet/projects/qagnn/data/

  model_configs:
    driver: local
