input {
	beats {
		port => 5044
	}
}

filter {
	if [myapp][application] != "MY-APPLICATION" {
		drop { }
	}
}

output {
	elasticsearch {
		hosts => "elasticsearch:9200"
		user => "elastic"
		password => "changeme"
		ecs_compatibility => disabled
		index => "my-application-logs-4"
	}
}
	
